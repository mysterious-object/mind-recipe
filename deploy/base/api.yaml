apiVersion: v1
kind: ServiceAccount
metadata:
  name: mind-recipe-api
  namespace: mind-recipe
automountServiceAccountToken: false
---
apiVersion: v1
kind: Service
metadata:
  name: mind-recipe-api
  namespace: mind-recipe
spec:
  selector:
    app: mind-recipe-api
  ports:
    - name: http
      port: 80
      targetPort: http
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mind-recipe-api
  namespace: mind-recipe
spec:
  replicas: 2
  selector:
    matchLabels:
      app: mind-recipe-api
  template:
    metadata:
      labels:
        app: mind-recipe-api
    spec:
      serviceAccountName: mind-recipe-api
      securityContext:
        runAsNonRoot: true
        seccompProfile: {type: RuntimeDefault}
      containers:
        - name: api
          image: REPLACE_WITH_IMMUTABLE_IMAGE
          imagePullPolicy: IfNotPresent
          ports: [{name: http, containerPort: 8080}]
          env:
            - name: MIND_RECIPE_ENV
              value: production
          envFrom:
            - secretRef:
                name: mind-recipe-runtime
          resources:
            requests: {cpu: 100m, memory: 256Mi}
            limits: {cpu: "1", memory: 768Mi}
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: {drop: [ALL]}
          readinessProbe:
            httpGet: {path: /healthz, port: http}
          livenessProbe:
            httpGet: {path: /healthz, port: http}
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: mind-recipe-api
  namespace: mind-recipe
spec:
  minAvailable: 1
  selector:
    matchLabels: {app: mind-recipe-api}
