apiVersion: v1
kind: Namespace
metadata:
  name: mind-nav-staging
  labels:
    app.kubernetes.io/name: mind-nav
    app.kubernetes.io/part-of: mind-recipe
---
apiVersion: v1
kind: Service
metadata:
  name: mind-nav-api
  namespace: mind-nav-staging
spec:
  selector:
    app: mind-nav-api
  ports:
    - name: http
      port: 80
      targetPort: http
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mind-nav-api
  namespace: mind-nav-staging
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: mind-nav-api
  template:
    metadata:
      labels:
        app: mind-nav-api
    spec:
      securityContext:
        runAsNonRoot: true
        fsGroup: 1000
        seccompProfile: {type: RuntimeDefault}
      containers:
        - name: api
          image: docker.io/library/fire-agents-local:ember-client-demo-20260808
          imagePullPolicy: IfNotPresent
          command: ["sh", "-c"]
          args: ["uvicorn app.main:app --host 0.0.0.0 --port 8080"]
          ports: [{name: http, containerPort: 8080}]
          env:
            - name: MIND_NAV_ENV
              value: development
            - name: MIND_NAV_DB_PATH
              value: /data/mind-nav-staging.sqlite3
            - name: PYTHONPATH
              value: /tmp/site:/app
          volumeMounts:
            - name: source
              mountPath: /app/app
              readOnly: true
            - name: staging-data
              mountPath: /data
            - name: tmp
              mountPath: /tmp
          resources:
            requests: {cpu: 50m, memory: 20Mi}
            limits: {cpu: "500m", memory: 384Mi}
          securityContext:
            runAsUser: 1000
            runAsGroup: 1000
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: {drop: [ALL]}
          readinessProbe:
            httpGet: {path: /healthz, port: http}
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet: {path: /healthz, port: http}
            initialDelaySeconds: 20
            periodSeconds: 20
      volumes:
        - name: source
          configMap:
            name: mind-nav-api-source
        - name: staging-data
          emptyDir: {}
        - name: tmp
          emptyDir: {}
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: mind-nav-api
  namespace: mind-nav-staging
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
spec:
  ingressClassName: traefik
  tls:
    - hosts: [staging-api.mindnav.142.93.201.156.sslip.io]
      secretName: mind-nav-api-tls
  rules:
    - host: staging-api.mindnav.142.93.201.156.sslip.io
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: mind-nav-api
                port: {name: http}
