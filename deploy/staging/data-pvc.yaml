apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mind-recipe-staging-data
  namespace: mind-recipe-staging
  labels:
    app.kubernetes.io/name: mind-recipe
spec:
  accessModes: [ReadWriteOnce]
  resources:
    requests:
      storage: 1Gi
